# Source Document Archive

`ZMeta Semantic Contract.docx` is a legacy source archive. The normative
semantic contract is `spec/semantics-contract.md`; use the Markdown spec,
schema, and policy files for implementation and conformance decisions.
