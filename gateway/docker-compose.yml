version: "3.8"
services:
  gateway:
    image: python:3.13-slim
    working_dir: /app
    command: >
      sh -c "pip install --no-cache-dir -r /app/gateway/requirements.txt
      && python /app/gateway/src/gateway.py --profile=H"
    volumes:
      - ..:/app:ro
    ports:
      - "5555:5555/udp"
