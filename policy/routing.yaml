routing:
  command_event:
    required_origin: SensorOps
    must_pass_through: SensorOps
  producers:
    Torch:
      allowed_event_types:
        - INFERENCE_EVENT
      forbidden_event_types:
        - COMMAND_EVENT
